{
  "name": "radio-pronto",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "search": "yarn workspace @pronto/search dev",
    "search:start": "yarn workspace @pronto/search start",
    "web": "yarn workspace @pronto/web dev",
    "web:start": "yarn workspace @pronto/web start",
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.0.11",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@changesets/cli": "^2.27.7"
  }
}
